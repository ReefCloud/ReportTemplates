% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_donut.R
\name{plot_donut}
\alias{plot_donut}
\title{Plot Donut for Benthic Cover (Defaults to HARD CORAL)}
\usage{
plot_donut(
  tier_id,
  cover_type = "HARD CORAL",
  show_labels = FALSE,
  label_format = c("percent", "count"),
  donut_width = 0.6
)
}
\arguments{
\item{tier_id}{Character. ReefCloud tier ID to query.}

\item{cover_type}{Character. Benthic group to plot (e.g., \code{"HARD CORAL"}, \code{"MACROALGAE"}, \code{"SOFT CORAL"}).
Default \code{"HARD CORAL"}.}

\item{show_labels}{Logical. Show per-segment labels (percentages or counts). Default \code{FALSE}.}

\item{label_format}{Character. Label type: \code{"percent"} or \code{"count"}. Default \code{"percent"}.}

\item{donut_width}{Numeric in (0, 1]. Ring thickness (e.g., \code{0.6}). Default \code{0.6}.}
}
\value{
A \code{ggplot} donut chart.
}
\description{
Retrieves survey compositions via \code{get_benthic_cover()}, filters to a requested
benthic \code{cover_type} (default \code{"HARD CORAL"}), classifies cover into ranges
using \code{add_cover_categories()}, and renders a whole-donut plot. Uses
\code{get_regional_summary()} to add a center label (region name and sites).
}
\details{
\itemize{
\item Detects the benthic group column among \code{c("type", "major_functional_group", "functional_group", "group", "category")}.
\item Classifies using the \code{"median"} cover column; adjust if you need \code{"mean"} etc.
\item Palette auto-maps from \code{cover_type} to \code{hc/ma/sc} (case-insensitive).
\item Factor order follows \code{add_cover_categories()} (descending for clean legends).
}
}
\examples{
# Default HARD CORAL donut for a tier:
# p <- plot_donut("exampleTierID")
# print(p)

# Show labels:
# p <- plot_donut("exampleTierID", cover_type = "MACROALGAE", show_labels = TRUE)

}
